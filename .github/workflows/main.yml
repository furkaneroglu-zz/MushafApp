name: "Mushaf iOS Derleme"

on:
  workflow_dispatch: 

jobs:
  build:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4 # v4 yaptık

      - name: Python Kur
        uses: actions/setup-python@v5 # v5 yaptık
        with:
          python-version: '3.11'

      - name: Flutter Kur
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'

      - name: Kütüphaneleri Yükle
        run: |
          pip install -r requirements.txt

      - name: iOS Paketi Yap (IPA)
        run: |
          flet build ipa --no-signing

      - name: Dosyayı Hazırla
        uses: actions/upload-artifact@v4 # İşte hata veren yer burasıydı, v4 yaptık
        with:
          name: Mushaf-iOS-Paketi
          path: build/ipa/*.ipa
